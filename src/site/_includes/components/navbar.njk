{%if settings.dgHomeLink === true%}
    <nav class="navbar">
        <div class="navbar-inner">
            <a href="/" style="text-decoration: none;">
                {% if meta.siteLogoPath %}
                    <img class="site-logo" src="{{meta.siteLogoPath}}" alt="{{meta.siteName}}">
                {% else %}
                    <h1 style="margin: 15px !important;">{{meta.siteName}}</h1>
                {% endif %}
            </a>
        </div>
        <div class="navbar-actions">
            <button class="theme-toggle" onclick="window.__setTheme(window.__theme === 'dark' ? 'light' : 'dark')" aria-label="Toggle theme">
                <i class="theme-toggle-icon" data-lucide="sun"></i>
            </button>
            {% if settings.dgEnableSearch === true%}
                {% include "components/searchButton.njk" %}
            {%endif%}
        </div>
    </nav>
    {%else%}
    <div class="empty-navbar" >
        <button class="theme-toggle" onclick="window.__setTheme(window.__theme === 'dark' ? 'light' : 'dark')" aria-label="Toggle theme">
            <i class="theme-toggle-icon" data-lucide="sun"></i>
        </button>
        {% if settings.dgEnableSearch === true%}
            {% include "components/searchButton.njk" %}
        {%endif%}
    </div>
{%endif%}